on:
  pull_request:
    branches:
      - main
      - dev
  push:
    branches:
      - main
      - dev
    tags:
      - "*"
  create:
  release:
    types:
      - published
  workflow_dispatch:

concurrency:
  group: ${{ github.event.pull_request.head.sha || github.ref }}
  cancel-in-progress: true

permissions: write-all

env:
  ZEPHYR_SDK_VERSION: "0.17.4"

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      sdk-hash: ${{ steps.generate-vars.outputs.sdk-hash }}
      sdk-revision: ${{ steps.generate-vars.outputs.sdk-revision }}
      repo: ${{ steps.generate-vars.outputs.repo }}
      repo-url: ${{ steps.generate-vars.outputs.repo-url }}
      branch: ${{ steps.generate-vars.outputs.branch }}
      repo-hash: ${{ steps.generate-vars.outputs.repo-hash }}
    steps:
      - name: 💻 Checkout Repository
        uses: actions/checkout@v6
        with:
          path: zephyr-workspace/SlimeNRF-CI
          submodules: recursive
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: 📟 Generate CI Variables
        id: generate-vars
        env:
          PR_HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          PR_HEAD_REPO_URL: ${{ github.event.pull_request.head.repo.clone_url }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          WEST_FILE="west.yml"

          if [ -f "$WEST_FILE" ]; then
            echo "Found west.yml in the root directory"
          elif [ -f "zephyr-workspace/$WEST_FILE" ]; then
            WEST_FILE="zephyr-workspace/$WEST_FILE"
            echo "Found west.yml in zephyr-workspace"
          elif [ -f "zephyr-workspace/SlimeNRF-CI/$WEST_FILE" ]; then
            WEST_FILE="zephyr-workspace/SlimeNRF-CI/$WEST_FILE"
            echo "Found west.yml in zephyr-workspace/SlimeNRF-CI"
          else
            echo "Error: west.yml not found!" >&2
            exit 1
          fi

          SDK_URL=$(grep -A5 "name: sdk-nrf" "$WEST_FILE" | grep "url:" | sed 's/.*url: *//')
          SDK_REVISION=$(grep -A5 "name: sdk-nrf" "$WEST_FILE" | grep "revision:" | sed 's/.*revision: *//')
          SDK_COMMIT=$(git ls-remote "$SDK_URL" "$SDK_REVISION" | awk '{print $1}')

          if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
            REPO="$PR_HEAD_REPO"
            REPO_URL="$PR_HEAD_REPO_URL"
            BRANCH="$PR_HEAD_REF"
            REPO_COMMIT="$PR_HEAD_SHA"
          else
            REPO="$GITHUB_REPOSITORY"
            REPO_URL="https://github.com/$GITHUB_REPOSITORY.git"
            BRANCH="${GITHUB_REF#refs/heads/}"
            REPO_COMMIT="$GITHUB_SHA"
          fi

          echo "sdk-hash=$SDK_COMMIT" >> "$GITHUB_OUTPUT"
          echo "sdk-revision=$SDK_REVISION" >> "$GITHUB_OUTPUT"
          echo "repo=$REPO" >> "$GITHUB_OUTPUT"
          echo "repo-url=$REPO_URL" >> "$GITHUB_OUTPUT"
          echo "branch=$BRANCH" >> "$GITHUB_OUTPUT"
          echo "repo-hash=$REPO_COMMIT" >> "$GITHUB_OUTPUT"

      - name: 🧰 Cache + Install Apt Dependencies
        id: cache-apt
        uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: >-
            git cmake ninja-build gperf dfu-util device-tree-compiler wget
            python3-dev python3-pip python3-setuptools python3-tk python3-wheel python3-venv
            xz-utils file make gcc gcc-multilib g++-multilib libsdl2-dev libmagic1
          version: 1.0

      - name: 📦 Cache Zephyr SDK
        id: cache-sdk
        uses: actions/cache@v5
        with:
          path: ~/zephyr-sdk-${{ env.ZEPHYR_SDK_VERSION }}
          key: zephyr-sdk-${{ env.ZEPHYR_SDK_VERSION }}

      - name: 🪁 Install Zephyr SDK
        id: install-sdk
        if: steps.cache-sdk.outputs.cache-hit != 'true'
        run: |
          wget -q https://github.com/zephyrproject-rtos/sdk-ng/releases/download/v${{ env.ZEPHYR_SDK_VERSION }}/zephyr-sdk-${{ env.ZEPHYR_SDK_VERSION }}_linux-x86_64_minimal.tar.xz
          tar xf zephyr-sdk-${{ env.ZEPHYR_SDK_VERSION }}_linux-x86_64_minimal.tar.xz -C ~/
          ~/zephyr-sdk-${{ env.ZEPHYR_SDK_VERSION }}/setup.sh -c -t arm-zephyr-eabi

      - name: 📦 Cache West Workspace
        id: cache-west
        uses: actions/cache@v5
        with:
          path: |
            zephyr-workspace/.west
            zephyr-workspace/zephyr
            zephyr-workspace/nrf
            zephyr-workspace/modules
            zephyr-workspace/nrfxlib
            zephyr-workspace/bootloader
            zephyr-workspace/tools
          key: west-workspace-${{ steps.generate-vars.outputs.sdk-hash }}

      - name: 📦 Cache Python venv
        id: cache-venv
        uses: actions/cache@v5
        with:
          path: ~/.venv
          key: venv-bootstrap-${{ env.ZEPHYR_SDK_VERSION }}

      - name: 🐍 Setup Python venv + West
        id: setup-venv
        if: steps.cache-venv.outputs.cache-hit != 'true'
        run: |
          python3 -m venv ~/.venv
          ~/.venv/bin/pip install west ninja

      - name: ♻️ Initialize Zephyr Workspace
        id: init-west
        run: |
          echo "$HOME/.venv/bin" >> $GITHUB_PATH
          export PATH="$HOME/.venv/bin:$PATH"
          cd zephyr-workspace
          if [ ! -d ".west" ]; then
            west init -l SlimeNRF-CI
          fi
          west update --narrow -o=--depth=1
          west zephyr-export

      - name: 📦 Cache PIP Requirements
        id: cache-pip
        uses: actions/cache@v5
        with:
          path: ~/.venv
          key: venv-pip-${{ env.ZEPHYR_SDK_VERSION }}-${{ hashFiles('zephyr-workspace/zephyr/scripts/requirements*.txt', 'zephyr-workspace/nrf/scripts/requirements-build.txt', 'zephyr-workspace/bootloader/mcuboot/scripts/requirements.txt') }}

      - name: 🐍 Install NCS, Zephyr, and MCUboot Python Requirements
        id: install-pip
        if: steps.cache-pip.outputs.cache-hit != 'true'
        run: |
          cd zephyr-workspace
          ~/.venv/bin/pip install -r zephyr/scripts/requirements.txt -r nrf/scripts/requirements-build.txt -r bootloader/mcuboot/scripts/requirements.txt

      - name: 📦 Cache Repository
        id: cache-repository
        uses: actions/cache@v5
        with:
          path: zephyr-workspace/SlimeVR-Tracker-nRF
          key: firmware-${{ steps.generate-vars.outputs.repo-hash }}

      - name: 📥 Clone Repository
        id: clone-repository
        if: steps.cache-repository.outputs.cache-hit != 'true'
        env:
          REPO_URL: ${{ steps.generate-vars.outputs.repo-url }}
          REPO_HASH: ${{ steps.generate-vars.outputs.repo-hash }}
        run: |
          cd zephyr-workspace
          rm -rf SlimeVR-Tracker-nRF

          git clone --filter=blob:none --no-checkout "$REPO_URL" SlimeVR-Tracker-nRF
          git -C SlimeVR-Tracker-nRF fetch --depth=1 origin "$REPO_HASH"
          git -C SlimeVR-Tracker-nRF checkout "$REPO_HASH"
          git -C SlimeVR-Tracker-nRF submodule update --init --recursive --depth 1

  build:
    needs: setup
    strategy:
      fail-fast: false
      matrix:
        boards: [
                  {boardname: "xiao_nrf54lm20a/nrf54lm20a/cpuapp", filename: "SlimeNRF_XIAO_nRF54LM20A_Tracker"},
                  {boardname: "xiao_nrf54lm20a/nrf54lm20a/cpuapp/sense", filename: "SlimeNRF_XIAO_nRF54LM20A_Sense_Tracker"},
                  {boardname: "xiao_ble/nrf52840", filename: "SlimeNRF_XIAO_Tracker"},
                  {boardname: "xiao_ble/nrf52840/sense", filename: "SlimeNRF_XIAO_Sense_Tracker"},
                  {boardname: "promicro_uf2/nrf52840/i2c", filename: "SlimeNRF_ProMicro_StackedSmol_Tracker_I2C"},
                  {boardname: "promicro_uf2/nrf52840/spi", filename: "SlimeNRF_ProMicro_StackedSmol_Tracker_SPI"},
                  {boardname: "promicro_uf2/nrf52840/smspi", filename: "SlimeNRF_ProMicro_StackedSmol_Tracker_SMSPI"},
                  {boardname: "promicro_uf2/nrf52840/601n1", filename: "SlimeNRF_ProMicro_StackedSmol_Tracker_601N1"},
                  {boardname: "promicro_uf2/nrf52840/lwkj_icm45v1", filename: "SlimeNRF_ProMicro_StackedSmol_Tracker_LWKJ_ICM45V1"},
                  # {boardname: "promicro_uf2/nrf52840/spi", filename: "SlimeNRF_ProMicro_StackedSmol_Tracker_SPI_SD", with_softdevice: true},
                  {boardname: "promicro_uf2/nrf52840/default_spi", filename: "SlimeNRF_ProMicro_Tracker_SPI"},
                  {boardname: "promicro_uf2/nrf52840/default_i2c", filename: "SlimeNRF_ProMicro_Tracker_I2C"},
                  {boardname: "promicro_uf2/nrf52840/chrysalis", filename: "SlimeNRF_ProMicro_Chrysalis_Tracker"},
                  {boardname: "styria_mini_uf2/nrf52840/spi", filename: "Styria_Mini_Tracker"},
                  # {boardname: "styria_mini_uf2/nrf52840/spi", filename: "Styria_Mini_Tracker_SD", with_softdevice: true},
                  {boardname: "foxsnacklite_uf2/nrf52840", filename: "FoxApplication_FoxSnackLite_Tracker"},
                  {boardname: "miro_uf2/nrf52833", filename: "Miro_Tracker"},
                  {boardname: "mochi_uf2/nrf52833", filename: "Mochi_Tracker"},
                  {boardname: "dawn_tracker_mini_uf2/nrf52833", filename: "Dawn_Tracker_Mini"},
                  {boardname: "aero_tracker_uf2/nrf52840", filename: "Aero_Tracker"},
                  {boardname: "aero_pro_uf2/nrf52840", filename: "Aero_Tracker_Pro"},
                  {boardname: "kawazu_uf2/nrf52833", filename: "Kawazu_Tracker"},
                  {boardname: "nini_slimevr_mag_uf2/nrf52833", filename: "NiNi_SlimeVR_MAG_Tracker"},
                  {boardname: "nekonya_tracker_uf2/nrf52833", filename: "nekonya_tracker_uf2"},
                  {boardname: "estmini_uf2/nrf52840", filename: "Estmini_Tracker"},
                  {boardname: "paper_smol_v1_uf2/nrf52833", filename: "paper_smol_v1_uf2"},
                ]
    runs-on: ubuntu-latest
    steps:
      - name: 💻 Checkout Repository
        uses: actions/checkout@v6
        with:
          path: zephyr-workspace/SlimeNRF-CI
          submodules: recursive
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: 🧰 Cache + Install Apt Dependencies
        id: restore-apt
        uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: >-
            git cmake ninja-build gperf dfu-util device-tree-compiler wget
            python3-dev python3-pip python3-setuptools python3-tk python3-wheel python3-venv
            xz-utils file make gcc gcc-multilib g++-multilib libsdl2-dev libmagic1
          version: 1.0

      - name: 📦 Restore Zephyr SDK
        id: restore-sdk
        uses: actions/cache/restore@v5
        with:
          path: ~/zephyr-sdk-${{ env.ZEPHYR_SDK_VERSION }}
          key: zephyr-sdk-${{ env.ZEPHYR_SDK_VERSION }}
          fail-on-cache-miss: true

      - name: 🪁 Register Zephyr SDK
        id: register-sdk
        run: |
          ~/zephyr-sdk-${{ env.ZEPHYR_SDK_VERSION }}/setup.sh -c -t arm-zephyr-eabi

      - name: 📦 Restore West Workspace
        id: restore-west
        uses: actions/cache/restore@v5
        with:
          path: |
            zephyr-workspace/.west
            zephyr-workspace/zephyr
            zephyr-workspace/nrf
            zephyr-workspace/modules
            zephyr-workspace/nrfxlib
            zephyr-workspace/bootloader
            zephyr-workspace/tools
          key: west-workspace-${{ needs.setup.outputs.sdk-hash }}
          fail-on-cache-miss: true

      - name: 📦 Restore Python venv
        id: restore-venv
        uses: actions/cache/restore@v5
        with:
          path: ~/.venv
          key: venv-pip-${{ env.ZEPHYR_SDK_VERSION }}-${{ hashFiles('zephyr-workspace/zephyr/scripts/requirements*.txt', 'zephyr-workspace/nrf/scripts/requirements-build.txt', 'zephyr-workspace/bootloader/mcuboot/scripts/requirements.txt') }}
          fail-on-cache-miss: true

      - name: ♻️ Register Zephyr Workspace
        id: register-west
        run: |
          echo "$HOME/.venv/bin" >> $GITHUB_PATH
          export PATH="$HOME/.venv/bin:$PATH"
          cd zephyr-workspace
          west zephyr-export

      - name: 📦 Restore Repository
        id: restore-repository
        uses: actions/cache/restore@v5
        with:
          path: zephyr-workspace/SlimeVR-Tracker-nRF
          key: firmware-${{ needs.setup.outputs.repo-hash }}
          fail-on-cache-miss: true

      - name: 🔨 Build SlimeVR-Tracker-nRF - ${{ matrix.boards.boardname }}
        id: build-firmware
        run: |
          cd zephyr-workspace
          sudo rm -rf Releases
          mkdir -p Releases
          REPO_DIR="${{ github.event.repository.name }}"
          if [ ! -d "$REPO_DIR" ]; then
            echo "Repo dir not found: $REPO_DIR; fallback to SlimeNRF-CI"
            REPO_DIR="SlimeNRF-CI"
          fi
          EXTRA_CMAKE_ARGS=()
          if [ "${{ matrix.boards.with_softdevice }}" = "true" ]; then
            EXTRA_CMAKE_ARGS+=(
              -DWITH_SOFTDEVICE=ON
              -DCONFIG_FLASH_LOAD_OFFSET=0x27000
            )
          fi
          rm -rf "$REPO_DIR/build"
          west build \
              --board ${{ matrix.boards.boardname }} \
              --pristine=always "$REPO_DIR" \
              --build-dir "$REPO_DIR/build" \
              -- \
              -DNCS_TOOLCHAIN_VERSION=NONE \
              -DBOARD_ROOT=../"$REPO_DIR" \
              "${EXTRA_CMAKE_ARGS[@]}"

          if [ "${{ matrix.boards.with_softdevice }}" = "true" ]; then
            for artifact in \
              "zephyr.hex:${{ matrix.boards.filename }}_OTA.hex" \
              "zephyr_sd.hex:${{ matrix.boards.filename }}.hex" \
              "zephyr_sd.uf2:${{ matrix.boards.filename }}.uf2"; do
              src_name="${artifact%%:*}"
              dst_name="${artifact#*:}"
              src="$REPO_DIR/build/$REPO_DIR/zephyr/$src_name"
              dst="Releases/$dst_name"
              if [ -f "$src" ]; then
                cp "$src" "$dst"
                echo "Saved: $dst"
              else
                echo "Not found (skip): $src"
              fi
            done
          else
            for ext in uf2 hex; do
              src="$REPO_DIR/build/$REPO_DIR/zephyr/zephyr.${ext}"
              dst="Releases/${{ matrix.boards.filename }}.${ext}"
              if [ -f "$src" ]; then
                cp "$src" "$dst"
                echo "Saved: $dst"
              else
                echo "Not found (skip): $src"
              fi
            done
          fi

      - name: 💾 Upload Artifact - ${{ matrix.boards.boardname }}
        id: upload-artifact
        if: ${{ steps.build-firmware.outcome == 'success' }}
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.boards.filename }}
          if-no-files-found: warn
          path: |
            zephyr-workspace/Releases/${{ matrix.boards.filename }}*

  publish-release:
    needs: build
    if: >-
      ${{
        needs.build.result == 'success'
        && (
          github.event_name == 'release'
          || github.ref_type == 'tag'
          || (github.event_name == 'create' && github.event.ref_type == 'tag')
        )
      }}
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
    env:
      RELEASE_TAG: ${{ github.event.release.tag_name || github.ref_name || github.event.ref }}
    steps:
      - name: 📥 Download Firmware Artifacts
        uses: actions/download-artifact@v7
        with:
          path: release-artifacts

      - name: Prefer UF2 over same-named HEX assets
        run: |
          while IFS= read -r -d '' hex_file; do
            uf2_file="${hex_file%.hex}.uf2"
            if [ -f "$uf2_file" ]; then
              echo "Skipping HEX because UF2 exists: $hex_file"
              rm "$hex_file"
            fi
          done < <(find release-artifacts -type f -name '*.hex' -print0)

      - name: 🚀 Upload Artifacts to GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          fail_on_unmatched_files: true
          files: |
            release-artifacts/**/*.uf2
